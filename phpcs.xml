<?xml version="1.0"?>
<ruleset name="PrettyPHP">
    <description>PrettyPHP coding standard</description>

    <!-- Show progress and use colors -->
    <arg value="p"/>
    <arg name="colors"/>

    <!-- Use PSR-12 as base -->
    <rule ref="PSR12">
        <!-- Allow multiple classes in test files -->
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>

    <!-- Allow multiple classes per file in test directories -->
    <rule ref="PSR1.Classes.ClassDeclaration.MultipleClasses">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>

    <!-- Set line length warning to 120 characters -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="150"/>
        </properties>
    </rule>
</ruleset>
